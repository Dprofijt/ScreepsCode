export enum RoleEnum {
  HARVESTER = "harvester",
  UPGRADER = "upgrader",
  BUILDER = "builder",
  MOVER = "mover",
  CLOSECOMBAT = "closeCombat",
  RANGECOMBAT = "rangeCombat",
  HEALER = "healer",
}
//"harvester" | "upgrader" | "builder" | "mover"